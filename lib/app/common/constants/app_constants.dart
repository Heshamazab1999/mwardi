import 'package:flutter/material.dart';

const domainURL = 'https://mwardi.com.sa';

// EndPoints pathes
const apiPath = '/public/api';
const apiVersion = '/v1';

const employeePath = '/employee';

const Map<String, Locale> supportedLocales = {
  "en": Locale("en", "US"),
  "ar": Locale("ar", "EG"),
};

const EdgeInsetsGeometry defaultPadding =
    EdgeInsets.only(top: 20, right: 20, left: 20);
const sizedBoxH=SizedBox(height: 15,);
const sizedBoxW=SizedBox(width: 15,);